const COLORS = {
    primary: '#000',
    background: '#fff',
    surface: '#fff',
    heading: '#000',
    text: '#222',
    lighterText: '#888'
};

const THEME = { Colors: COLORS };

export default THEME;